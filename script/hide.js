$(document).ready(function(){$("#all").keypress(function(){var isempty =  $("#all").val();var myregsxp = /[^а-€$]/g; // ¬ общем это регул€рное выражениеvar answear = isempty.match(myregsxp); // »щем совпадени€if(isempty.length > 0 && answear==null){ // ѕровер€м совпадени€ и количество знаков в password$("#all").next().empty().show(20000); // Ёфaект jquery$("#all").after("<FONT style='color:red;'>Ќеправельний вв≥д</FONT>");$("#all").next().fadeOut(3000); // Ёфaект jquery}});});$(document).ready(function(){$("#all2").keypress(function(){var isempty =  $("#all2").val();var myregsxp = /[^а-€$]/g; // ¬ общем это регул€рное выражениеvar answear = isempty.match(myregsxp); // »щем совпадени€if(isempty.length > 0 && answear==null){ // ѕровер€м совпадени€ и количество знаков в password$("#all2").next().empty().show(20000); // Ёфaект jquery$("#all2").after("<FONT style='color:red;'>Ќеправельний вв≥д</FONT>");$("#all2").next().fadeOut(3000); // Ёфaект jquery}});});$(document).ready(function(){$("#all3").keypress(function(){var isempty =  $("#all3").val();var myregsxp = /[^0-9$]/g; // ¬ общем это регул€рное выражениеvar answear = isempty.match(myregsxp); // »щем совпадени€if(isempty.length > 0 && answear!==null){ // ѕровер€м совпадени€ и количество знаков в password$("#all3").next().empty().show(20000); // Ёфaект jquery$("#all3").after("<FONT style='color:red;'>Ќеправельний вв≥д</FONT>");$("#all3").next().fadeOut(3000); // Ёфaект jquery}});});